dependencies {
	compile project(':MayDeskBase')
	compile project(':MayDeskSocial')	
}

jar {
	manifest.attributes provider: 'gradle'
}
	
war {
	classpath = jar.outputs.files + configurations.runtime - project(':MayDeskBase').configurations.providedCompile
}
